import React from 'react';
import useTitle from '../../Hooks/useTitle';

const MyReviews = () => {
    useTitle('My Reviews')
    return (
        <div>
            <h1>Review Here</h1>
        </div>
    );
};

export default MyReviews;